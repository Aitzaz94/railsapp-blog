<h1 align="center" > Categories here ... </h1>

<div align="center row" >
    <%= will_paginate %>
    <% @categories.each do |category| %>
        <ul class="listing" >
            <div>
                <div class="well col-md-4 col-md-offset-4">
                    <li class="article-title">
                        <%= link_to "#{category.name}", category_path(category) %>
                    </li>
                </div>
            </div>
        </ul>
    <% end %>
    <%= will_paginate %>
</div>